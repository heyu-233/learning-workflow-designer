# FreeRTOS 轻量示例练习

## 第 1 章：FreeRTOS 项目全局地图

### 题 1：排序后解释（5 分）

下面是一个最小 FreeRTOS 应用从启动到任务运行的步骤，它们现在顺序被打乱：

1. `vTaskStartScheduler()`
2. 创建 LED 任务和串口任务
3. 硬件时钟、GPIO、串口初始化
4. 任务进入 `for(;;)` 循环并通过阻塞 API 让出 CPU

请完成：

1. 写出正确顺序。
2. 解释为什么创建任务必须发生在调度器启动之前。
3. 如果任务循环里没有阻塞或延时，低优先级任务可能出现什么现象？

答：

________________________________________________________________________________

## 第 2 章：任务状态与调度入口

### 题 1：对照辨析（5 分）

方案 A：高优先级任务轮询标志位，直到数据到达。

方案 B：高优先级任务等待队列或信号量，数据到达后被唤醒。

请比较：

1. 两者对 CPU 占用的差异是什么？
2. 哪个方案更符合 FreeRTOS 的任务调度方式？为什么？
3. 方案 A 在什么极端情况下可能暂时可用？

答：

________________________________________________________________________________

### 题 2：任务模型边界（5 分）

请说明任务函数为什么通常不能 `return`，并写出任务主动结束时应该调用的 API。

答：

________________________________________________________________________________

## 第 3 章：优先级、时间片和阻塞

### 题 1：调度判断（5 分）

两个同优先级任务都处于就绪态，系统开启时间片调度。请说明它们会如何共享 CPU，并指出其中一个任务调用 `vTaskDelay()` 后状态如何变化。

答：

________________________________________________________________________________

## 第 4 章：队列通信链路

### 题 1：链路纠错（10 分）

下面的队列通信链路有一处错误：

```text
中断服务函数 -> 直接调用 xQueueSend() -> 任务调用 xQueueReceive() -> 处理消息
```

请完成：

1. 标出错误位置。
2. 写出更合适的 FreeRTOS API 链路。
3. 说明这个错误可能导致怎样的运行风险。

答：

________________________________________________________________________________

## 第 5 章：信号量与事件同步

### 题 1：对照辨析（6 分）

请比较二值信号量和事件组：它们分别适合表达什么同步关系？如果一个任务要等待多个事件同时成立，应该优先考虑哪一种？

答：

________________________________________________________________________________

## 第 6 章：互斥锁与优先级继承

### 题 1：故障分析（6 分）

低优先级任务持有共享资源，高优先级任务等待该资源，中优先级任务持续运行。请解释这是什么问题，以及 FreeRTOS 互斥锁如何缓解它。

答：

________________________________________________________________________________

## 第 7 章：堆管理方案

### 题 1：方案选择（4 分）

如果项目需要频繁创建和删除不同大小的对象，`heap_1` 为什么不合适？可以优先考虑哪类 heap 方案？

答：

________________________________________________________________________________

## 第 8 章：任务栈和溢出检查

### 题 1：排查顺序（4 分）

任务偶发 HardFault，怀疑栈溢出。请写出至少三个排查动作。

答：

________________________________________________________________________________

## 第 9 章：运行态观测与日志

### 题 1：观测设计（5 分）

请设计一个最小运行态观测方案，用来确认任务是否被饿死、队列是否堆积、以及某个同步点是否长期等待。

答：

________________________________________________________________________________

## 第 10 章：综合调试演练

### 题 1：综合链路（5 分）

串口中断收到命令后，控制任务没有响应。请按“中断 -> 队列 -> 任务状态 -> 同步资源 -> 日志”的顺序写出诊断链路。

答：

________________________________________________________________________________
